local_costmap:
  global_frame: odom
  robot_base_frame: base_link

  update_frequency: 5.0
  publish_frequency: 5.0
  transform_tolerance: 0.5  

  static_map: true  
  rolling_window: true
  width: 3
  height: 3
  resolution: 0.05

  plugins:
  - {name: obstacle_layer, type: "costmap_2d::VoxelLayer"}
  # - {name: lane_layer, type: "costmap_2d::VoxelLayer"}
  - {name: inflation_layer, type: "costmap_2d::InflationLayer"}
  - {name: sonar_layer,   type: "range_sensor_layer::RangeSensorLayer"}
  
  
  #Configuration for the sensors that the costmap will use to update a map
  obstacle_layer:   
    observation_sources: scan
    scan: {sensor_frame: base_scan, data_type: LaserScan, topic: scan, marking: true, clearing: true}

  sonar_layer:
    topics: ["/us_back", "/us_right", "/us_left", "/us_back_left", "/us_back_right"]
    clear_on_max_reading: true
  
  lane_layer:
    observation_sources: lane_scan
    lane_scan: {sensor_frame: base_scan, data_type: LaserScan, topic: lane_scan, marking: false, clearing: true}

